{
  "scripts": {
    "postinstall": "(cd frontend && cwd && ls -la && npm install --loglevel verbose) && cwd && ls -la ",
    "heroku-postbuild": "(cd frontend && cwd && ls -la && npm run build --loglevel verbose)"
  },
  "engines": {
    "node": "12.22.6",
    "npm": "7.24.1"
  }
}
