{
  "scripts": {
    "postinstall": "(cd frontend && pwd && ls -la && npm install --loglevel verbose) && pwd && ls -la ",
    "heroku-postbuild": "(cd frontend && pwd && ls -la && npm run build --loglevel verbose)"
  },
  "engines": {
    "node": "12.22.6",
    "npm": "7.24.1"
  }
}
