{
  "scripts": {
    "postinstall": "(cd frontend && npm install --loglevel verbose && ls -la node_modules && ls -la node_modules/.bin)",
    "heroku-postbuild": "(cd frontend && ls -la node_modules && ls -la node_modules/.bin && npm run build --loglevel verbose)"
  },
  "engines": {
    "node": "12.22.6",
    "npm": "7.24.1"
  }
}
